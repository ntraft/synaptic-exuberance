name: synaptic-exuberance
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - numpy
  - pandas
  - scipy
  - matplotlib
  - notebook
  - seaborn
  - tqdm
  - ffmpeg
  - moviepy
  - python-graphviz
  - gym-box2d
  - pyglet
  - swig
  - pip:
    - gizeh
    # Don't install neat-python from pip, because you'll get a version from 2017.
    # Or, you can install it and change the source to be compatible.
    # - neat-python
